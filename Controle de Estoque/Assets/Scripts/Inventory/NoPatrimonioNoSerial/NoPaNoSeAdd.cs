using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class NoPaNoSeAdd : MonoBehaviour
{
    private NoPaNoSeItem itemToAdd = new NoPaNoSeItem();
    [SerializeField] private TMP_InputField itemNameInput;
    [SerializeField] private TMP_InputField itemQuantity;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
